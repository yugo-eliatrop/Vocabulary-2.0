@using Vocabulary
@{
    Word word = Model == null ? new Word() : Model as Word;
    ViewData["Title"] = "Add new word";
    ViewBag.Word = word;
    ViewBag.Action = "/Words/Create";
}

<div class="text-center">
    <h4 class="display-5">
        Add Word
    </h4>
</div>

@await Html.PartialAsync("_Form.cshtml")
